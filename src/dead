<Grid container spacing={2}>
    <Grid item xs={6}>
        <ImageListItem key={url} sx={{}}>
            <img
                srcSet={`${url}`}
                src={`${url}`}
                alt={name}
                loading="lazy"
            
            />
        </ImageListItem>
    </Grid>
    <Grid item xs={6}>
        <Stack>
            <Typography> {name}</Typography>
            {/* <p> {description} </p> */}
        </Stack>
        
    </Grid>
    <Grid item xs={8}>
        <Typography> cost: ${cost}</Typography>
    </Grid>
    <Grid item xs={4}>
        <Button>Button</Button>
    </Grid>
 </Grid>